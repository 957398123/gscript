function print(msg, flag){
    if(flag){
        println("this msg=" + this[msg]);  // 传进来的是变量名
    }else{
        println("msg:" + msg);  // 预设定打印函数
    }
}

var a = 1; // 全局变量a
var b = {"name" : "布鲁斯李"};
b.printMessage = print;
b.printMessage("name");
b.printMessage("name", true);
print(a);